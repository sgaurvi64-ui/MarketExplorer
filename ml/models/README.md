# Models

Saved model artifacts should go here.

Expected baseline outputs:

- `baseline_stock_model.joblib`
- `baseline_stock_model_meta.json`

Django can later load these files when `ML_MODE` is set to `model`.
