# Datasets

Put CSV files and raw market feature tables here.

Suggested files later:

- `indian_stocks_sample.csv`
- `daily_features.csv`
- `sector_summary.csv`

You can start by generating a sample training file with:

```powershell
python scripts/generate_sample_dataset.py
```
